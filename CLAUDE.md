# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`wncrazy/wncrazy`) currently has no commits and no files — it is empty. There is no codebase yet to describe in terms of build/lint/test commands or architecture.

When code is added to this repository, update this file to document:
- Commands to build, lint, and run tests (including running a single test)
- The high-level architecture and structure of the codebase
